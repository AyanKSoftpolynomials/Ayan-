# Orange Tree LMS Frontend

This folder contains the React frontend for the LMS backend.

## Setup

1. Install dependencies:
   ```bash
   npm install
   ```
2. Create a local environment file:
   ```bash
   copy .env.example .env
   ```
3. Start the app:
   ```bash
   npm run dev
   ```

By default the frontend targets `http://localhost:5000`.
